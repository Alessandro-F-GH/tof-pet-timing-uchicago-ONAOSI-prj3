# Report figures

This directory contains the figures used by `report/main.tex`.

Use PDF for vector plots when possible. Figures derived from analysis results should correspond to the same study outputs used for the numerical tables.

The experimental setup image is stored locally as `experimental_setup.png` so the report is self-contained.
